---
phase: 07-integration-fixes-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app/index.tsx, app/history.tsx, app/stats.tsx, src/storage/mmkv.ts, src/storage/index.ts]
autonomous: true
---

<objective>
Fix cross-phase wiring issues and remove dead code: add useFocusEffect to home/history/stats screens so data refreshes on navigation return, fix custom emoji lookup in history and stats, and remove unused MMKV storage module.

Purpose: Close all integration gaps from v1-MILESTONE-AUDIT.md — the last phase before the app is fully shippable.
Output: All list screens refresh on focus, custom emojis display correctly everywhere, no dead code.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1-MILESTONE-AUDIT.md

# Established pattern for useFocusEffect (from manage.tsx):
# import { router, useFocusEffect } from 'expo-router';
# import { useCallback } from 'react';
# useFocusEffect(useCallback(() => { setMappings(getCustomMappings()); }, []));

# Custom mapping lookup (from customMappings.ts):
# getCustomMappings() returns CustomMapping[] with id, label, emoji fields

@app/index.tsx
@app/history.tsx
@app/stats.tsx
@src/storage/customMappings.ts
@src/storage/mmkv.ts
@src/storage/index.ts
@src/data/mappings.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire useFocusEffect on home, history, and stats screens + fix custom emoji lookup</name>
  <files>app/index.tsx, app/history.tsx, app/stats.tsx</files>
  <action>
**index.tsx (home screen):**
- Import `useFocusEffect` from `expo-router` and `useState`, `useCallback` from `react`
- Move `customCravings` and `allCravings` into state: `const [allCravings, setAllCravings] = useState<CravingItem[]>(CRAVINGS)`
- Add `useFocusEffect(useCallback(() => { ... rebuild allCravings from CRAVINGS + getCustomMappings() and setAllCravings ... }, []))` so the grid refreshes when returning from manage/add-mapping screens
- Keep the synchronous initial value (CRAVINGS) so first render is instant — useFocusEffect adds custom mappings on focus

**history.tsx:**
- Replace `useEffect` with `useFocusEffect` + `useCallback` (import from `expo-router` and `react`) so history refreshes on every screen visit
- Fix emoji lookup: after looking up in CRAVINGS, also check `getCustomMappings()` for custom craving emoji. Import `getCustomMappings` from `../src/storage/customMappings`. Change the lookup to: `const cravingEntry = CRAVINGS.find(c => c.id === item.craving_id); const customEntry = !cravingEntry ? getCustomMappings().find(m => String(m.id) === item.craving_id) : null; const emoji = cravingEntry?.emoji ?? customEntry?.emoji ?? '❓';`

**stats.tsx:**
- Replace `useEffect` with `useFocusEffect` + `useCallback` (same pattern as history.tsx)
- Fix emoji lookup in BOTH the Top Cravings map and What Helped map: same pattern as history.tsx — check CRAVINGS first, fall back to getCustomMappings(), then '❓'
- Import `getCustomMappings` from `../src/storage/customMappings`
- To avoid calling getCustomMappings() inside every map iteration, fetch it once in the useFocusEffect callback and store in state alongside topCravings/feedbackStats. Or simpler: create a `const customMappings = getCustomMappings()` at the top of the component (sync call, cheap). Then use it in both map blocks.
  </action>
  <verify>npx tsc --noEmit (TypeScript compiles with 0 errors); visually confirm: navigate home → manage → add custom craving → back → home shows new craving; navigate to history/stats → back → history/stats data refreshes</verify>
  <done>All three screens use useFocusEffect; custom emoji renders correctly in history and stats; home grid includes freshly added custom cravings on return</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead MMKV storage code</name>
  <files>src/storage/mmkv.ts, src/storage/index.ts</files>
  <action>
- Delete `src/storage/mmkv.ts` entirely (MMKV was scaffolded in Phase 1 but all persistence uses SQLite — the module is never imported by any screen or feature code)
- Edit `src/storage/index.ts`: remove the `export { storage } from './mmkv'` line. Keep the sqlite and events exports.
- Verify no other file imports from mmkv.ts (grep confirms nothing imports it outside index.ts barrel)
  </action>
  <verify>npx tsc --noEmit (still compiles clean); grep -r "mmkv" src/ app/ returns no hits except possibly the deleted file reference</verify>
  <done>mmkv.ts deleted, index.ts barrel cleaned, no import errors, no dead code</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes with 0 errors
- [ ] `npx expo export --platform ios` (or `npm run build` equivalent) succeeds
- [ ] No files import from deleted mmkv.ts
- [ ] All three data screens (home, history, stats) use useFocusEffect
- [ ] Custom emoji displays correctly in history and stats for custom mappings
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- v1-MILESTONE-AUDIT integration issues fully resolved
- Dead MMKV code removed
  </success_criteria>

<output>
After completion, create `.planning/phases/07-integration-fixes-cleanup/07-01-SUMMARY.md`
</output>
