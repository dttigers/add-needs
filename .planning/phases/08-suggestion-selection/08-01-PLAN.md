---
phase: 08-suggestion-selection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/storage/events.ts
  - app/result.tsx
autonomous: true
---

<objective>
Make suggestions tappable on the result screen so users can indicate which suggestion they'll try, plus an "Other" option. This replaces the current yes/no/skip feedback with richer per-suggestion feedback data.

Purpose: Better feedback loop — instead of "did the need help?" the app learns which *specific suggestion* works for each craving, enabling smarter ordering over time.
Output: Updated result screen with tappable suggestions and updated storage schema.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@app/result.tsx
@src/storage/events.ts
@src/data/mappings.ts
@src/theme.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add chosen_suggestion column to events storage</name>
  <files>src/storage/events.ts</files>
  <action>
  1. Add a migration to add `chosen_suggestion` column (nullable TEXT) to redirect_events table:
     - Use `ALTER TABLE redirect_events ADD COLUMN chosen_suggestion TEXT` wrapped in a try/catch (column may already exist on re-run)
     - Place after the existing CREATE TABLE statement

  2. Update the RedirectEvent interface to include `chosen_suggestion: string | null`

  3. Add a new function `setChosenSuggestion(eventId: number, suggestion: string): void` that updates the chosen_suggestion column for a given event. This also sets feedback to 'yes' since choosing a suggestion implies it helped.

  4. Update `setFeedback` to also accept 'other' as a feedback value (for when user picks "Other / none of these").

  5. Add a new query function `getSuggestionStats(cravingId: string): { suggestion: string, count: number }[]` that returns which suggestions have been chosen most for a given craving, ordered by count descending. This enables future suggestion reordering.

  AVOID:
  - Do NOT change existing logEvent signature — chosen_suggestion is set after initial log, same as feedback
  - Do NOT break existing getHistory/getFeedbackStats queries — chosen_suggestion is additive
  - Do NOT remove the existing feedback column — it still has value for "other"/"skip" cases
  </action>
  <verify>
  - TypeScript compiles: npx tsc --noEmit (or npx expo lint)
  - RedirectEvent interface includes chosen_suggestion
  - setChosenSuggestion function exported
  - getSuggestionStats function exported
  </verify>
  <done>Storage layer supports recording which suggestion was chosen per event</done>
</task>

<task type="auto">
  <name>Task 2: Make suggestions tappable with "Other" option on result screen</name>
  <files>app/result.tsx</files>
  <action>
  1. Replace the static suggestion Views with TouchableOpacity components. When tapped:
     - Call setChosenSuggestion(eventId, suggestion text)
     - Navigate back to home (router.replace('/'))
     - Add brief visual feedback on tap (use the accent color border or background highlight)

  2. Add an "Other / none of these" option below the suggestions:
     - Styled as a subtle tertiary button (surface background, muted text)
     - On tap: call setFeedback(eventId, 'other'), then router.replace('/')

  3. Remove the existing "Did that help?" yes/no/skip feedback section. The suggestion selection replaces it:
     - Tapping a suggestion = "yes, this specific one"
     - Tapping "Other" = "no, none of these"
     - The skip case is handled by the user just pressing back/going home without tapping anything (feedback stays null)

  4. Update the section header from "Try this" to "What will you try?" to make the tappability obvious.

  5. Style the tappable suggestions:
     - Keep the existing card look (surface background, rounded corners)
     - Add a subtle right-arrow or chevron indicator to signal tappability
     - Use activeOpacity={0.7} for touch feedback
     - Keep the numbered format (1. suggestion, 2. suggestion)

  AVOID:
  - Do NOT add animation or complex state — keep it snappy (under 10 second total flow)
  - Do NOT change the top section (craving → need display) — it works well
  - Do NOT remove the adaptive note section — it still provides value
  </action>
  <verify>
  - App builds: npx expo lint or npx expo start (no errors on load)
  - Result screen shows tappable suggestion cards
  - "Other" option visible below suggestions
  - Tapping a suggestion navigates back to home
  - No "Did that help?" section remains
  </verify>
  <done>Suggestions are tappable, "Other" option exists, old feedback section removed, flow stays under 10 seconds</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] App starts without errors (npx expo start)
- [ ] Navigate to result screen — suggestions render as tappable cards
- [ ] Tap a suggestion — records chosen_suggestion, returns to home
- [ ] Tap "Other" — records feedback as 'other', returns to home
- [ ] Press back without tapping — feedback stays null (skip behavior)
- [ ] History screen still works (no breaking schema changes)
- [ ] Stats screen still works
</verification>

<success_criteria>

- Suggestions are tappable on result screen
- "Other" option available for when neither suggestion fits
- Chosen suggestion stored in database per event
- Existing history and stats views unbroken
- Total flow (craving → need → suggestion tap → home) stays under 10 seconds
  </success_criteria>

<output>
After completion, create `.planning/phases/08-suggestion-selection/08-01-SUMMARY.md`
</output>
