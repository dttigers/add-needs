---
phase: 06-adaptive-suggestions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/storage/events.ts
  - app/result.tsx
autonomous: true

must_haves:
  truths:
    - "After 3+ rated redirects for a craving, the result screen shows a positive note when >=60% were helpful"
    - "After 5+ rated redirects for a craving, the result screen shows a struggling note when <30% were helpful"
    - "When fewer than 3 rated redirects exist, no adaptive note is shown"
    - "Adaptive note appears between suggestions and feedback sections"
  artifacts:
    - path: "src/storage/events.ts"
      provides: "getCravingFeedbackSummary function"
      exports: ["getCravingFeedbackSummary"]
    - path: "app/result.tsx"
      provides: "Adaptive note UI on result screen"
      contains: "adaptiveNote"
  key_links:
    - from: "app/result.tsx"
      to: "src/storage/events.ts"
      via: "import getCravingFeedbackSummary"
      pattern: "getCravingFeedbackSummary"
    - from: "app/result.tsx"
      to: "adaptiveNote state"
      via: "useState + useEffect sets note based on feedback summary"
      pattern: "setAdaptiveNote"
---

<objective>
Add feedback-driven adaptive notes to the result screen so users see whether a particular craving-to-need mapping has historically helped them.

Purpose: Closes DATA-05 requirement — the app adjusts what it shows based on accumulated user feedback for each craving. This was planned for Phase 4 (plan 04-02) but never executed.
Output: `getCravingFeedbackSummary` query in events.ts, conditional adaptive note rendered in result.tsx
</objective>

<execution_context>
@/Users/jamesonmorrill/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamesonmorrill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-adaptive-suggestions/06-RESEARCH.md
@src/storage/events.ts
@app/result.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add getCravingFeedbackSummary query to events.ts</name>
  <files>src/storage/events.ts</files>
  <action>
Add a new exported interface and function to src/storage/events.ts after the existing getFeedbackStats function (end of file):

1. Add interface CravingFeedbackSummary with fields: yes: number, no: number, skip: number, total: number

2. Add function getCravingFeedbackSummary(cravingId: string): CravingFeedbackSummary
   - SQL query: SELECT SUM(CASE WHEN feedback = 'yes' THEN 1 ELSE 0 END) as yes, SUM(CASE WHEN feedback = 'no' THEN 1 ELSE 0 END) as no, SUM(CASE WHEN feedback = 'skip' THEN 1 ELSE 0 END) as skip, COUNT(*) as total FROM redirect_events WHERE craving_id = ?
   - Use db.getAllSync with [cravingId] parameter
   - Return rows[0] ?? { yes: 0, no: 0, skip: 0, total: 0 } to handle NULL aggregate on empty result set
   - Use sync pattern (db.getAllSync) — no async/await
  </action>
  <verify>npx tsc --noEmit passes with zero errors</verify>
  <done>getCravingFeedbackSummary exported from events.ts, returns aggregate feedback counts for a given craving_id</done>
</task>

<task type="auto">
  <name>Task 2: Add adaptive note UI to result.tsx</name>
  <files>app/result.tsx</files>
  <action>
Modify app/result.tsx to show a conditional adaptive note between the suggestions and feedback sections:

1. Add import: import getCravingFeedbackSummary from '../src/storage/events' (add to existing import — destructured alongside logEvent and setFeedback)

2. Add state BEFORE the early-return guard (line 37) — hooks before guards rule:
   const [adaptiveNote, setAdaptiveNote] = useState<string | null>(null);
   Place it right after the existing useState for eventId (line 28).

3. Add getCravingFeedbackSummary call INSIDE the existing useEffect (lines 30-35), after the logEvent call:
   ```
   const summary = getCravingFeedbackSummary(cravingId);
   const ratedTotal = summary.yes + summary.no;
   if (ratedTotal >= 3 && (summary.yes / ratedTotal) >= 0.6) {
     setAdaptiveNote('This suggestion has helped you before — give it a try!');
   } else if (ratedTotal >= 5 && (summary.yes / ratedTotal) < 0.3) {
     setAdaptiveNote('This one hasn\'t clicked yet — maybe try something different after.');
   }
   ```
   Do NOT create a second useEffect. Add to the existing one.

4. Add conditional render between the suggestions section (ends ~line 91) and the feedback section (starts ~line 93):
   ```
   {adaptiveNote && (
     <View style={{ marginTop: 20, backgroundColor: colors.surface, borderRadius: 12, padding: 16, borderLeftWidth: 3, borderLeftColor: colors.accent }}>
       <Text style={{ color: colors.textMuted, fontSize: 11, fontWeight: '600', textTransform: 'uppercase', letterSpacing: 1, marginBottom: 6 }}>
         Based on your history
       </Text>
       <Text style={{ color: colors.textSecondary, fontSize: 15, lineHeight: 21 }}>
         {adaptiveNote}
       </Text>
     </View>
   )}
   ```

Key constraints:
- Use colors.textMuted and colors.textSecondary from the existing useThemeColors() hook (already imported)
- Do NOT use hardcoded color values
- Do NOT use async/await — getCravingFeedbackSummary is synchronous
- Import from '../src/storage/events' (direct path, not barrel)
  </action>
  <verify>npx tsc --noEmit passes with zero errors; visually confirm adaptive note appears on result screen after providing 3+ yes feedbacks for the same craving</verify>
  <done>Result screen conditionally shows an adaptive note between suggestions and feedback sections based on historical feedback data for the current craving</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero TypeScript errors
2. App launches without crash: `npx expo start`, navigate to a craving result
3. First-time craving: no adaptive note visible (expected — no history)
4. After 3+ "yes" feedbacks on same craving: positive adaptive note appears ("has helped you before")
5. After 5+ redirects with mostly "no" feedback: struggling note appears ("hasn't clicked yet")
</verification>

<success_criteria>
- getCravingFeedbackSummary function exists and returns correct aggregate counts
- Result screen shows adaptive note when threshold conditions are met
- No adaptive note shown when insufficient feedback history
- No TypeScript errors, no runtime crashes
- DATA-05 requirement satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/06-adaptive-suggestions/06-01-SUMMARY.md`
</output>
