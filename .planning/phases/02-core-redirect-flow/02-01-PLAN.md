---
phase: 02-core-redirect-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/data/mappings.ts, app/index.tsx]
autonomous: true
---

<objective>
Create the craving data layer and replace the placeholder home screen with a tappable craving selection grid.

Purpose: Establish the content foundation (craving-to-need mappings) and build the first interactive screen — user taps a craving and gets routed to the result screen.
Output: src/data/mappings.ts with 12 default ADHD cravings and their need mappings; app/index.tsx updated to a scrollable 2-column grid of tappable craving tiles with a minimal inline UX hint.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@app/_layout.tsx
@app/index.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create craving data layer</name>
  <files>src/data/mappings.ts</files>
  <action>
Create src/data/mappings.ts with the following TypeScript types and default data:

```typescript
export interface CravingItem {
  id: string;
  label: string;
  emoji: string;
}

export interface NeedResult {
  need: string;
  suggestions: [string, string]; // exactly 2 suggestions
}
```

Export `CRAVINGS: CravingItem[]` with these 12 items (in this order):
1. { id: 'sugar', label: 'Sugar / sweets', emoji: '🍬' }
2. { id: 'caffeine', label: 'Caffeine / coffee', emoji: '☕' }
3. { id: 'scrolling', label: 'Social media', emoji: '📱' }
4. { id: 'snacks', label: 'Junk food', emoji: '🍟' }
5. { id: 'shopping', label: 'Impulse purchase', emoji: '🛒' }
6. { id: 'gaming', label: 'Video games', emoji: '🎮' }
7. { id: 'alcohol', label: 'Alcohol', emoji: '🍺' }
8. { id: 'nicotine', label: 'Nicotine', emoji: '🚬' }
9. { id: 'procrastinating', label: 'Procrastinating', emoji: '😮‍💨' }
10. { id: 'restless', label: 'Restlessness', emoji: '🦶' }
11. { id: 'binging', label: 'Binge-watching', emoji: '📺' }
12. { id: 'impulse-chat', label: 'Impulsive texting', emoji: '💬' }

Export `MAPPINGS: Record<string, NeedResult>` with these entries:
- 'sugar': { need: 'Energy regulation', suggestions: ['Eat a protein snack', 'Drink a full glass of water'] }
- 'caffeine': { need: 'Mental activation', suggestions: ['Take a 5-min movement break', 'Splash cold water on your face'] }
- 'scrolling': { need: 'Stimulation or escape', suggestions: ['Put your phone in another room for 10 min', 'Text one specific person intentionally'] }
- 'snacks': { need: 'Sensory stimulation', suggestions: ['Eat something crunchy and healthy', 'Chew gum'] }
- 'shopping': { need: 'Novelty or accomplishment', suggestions: ['Add to a wishlist and wait 24h', 'Complete one small task first'] }
- 'gaming': { need: 'Mastery or escape', suggestions: ['Break a real task into steps like levels', 'Watch a 5-min skill tutorial'] }
- 'alcohol': { need: 'Relaxation or connection', suggestions: ['Call someone you like', 'Take a warm shower'] }
- 'nicotine': { need: 'Pause and reset', suggestions: ['Step outside without a cigarette', 'Take 5 slow deep breaths'] }
- 'procrastinating': { need: 'Clarity on what to do', suggestions: ['Write the single next physical step', 'Set a 2-min timer and just start'] }
- 'restless': { need: 'Movement or sensory input', suggestions: ['Go for a 5-min walk', 'Do 10 jumping jacks'] }
- 'binging': { need: 'Rest or emotional escape', suggestions: ['Take a real 20-min nap', 'Write down what you\'re avoiding'] }
- 'impulse-chat': { need: 'Connection or validation', suggestions: ['Write what you want to say and wait 10 min', 'Call one person intentionally'] }

No default export. Named exports only. TypeScript strict mode — no `any`, all types explicit.
  </action>
  <verify>npx tsc --noEmit passes with 0 errors from the project root</verify>
  <done>src/data/mappings.ts exists, exports CRAVINGS (12 items) and MAPPINGS (12 entries), TypeScript compiles clean</done>
</task>

<task type="auto">
  <name>Task 2: Build craving selection screen</name>
  <files>app/index.tsx</files>
  <action>
Replace app/index.tsx with a craving selection screen. Keep the dark background (#0a0a0a) and "What are you craving?" headline from the Phase 1 shell. Add:

**Layout:**
- StatusBar hidden
- Full-screen dark container (flex: 1, backgroundColor: '#0a0a0a')
- Top section: headline "What are you craving?" (same style as before)
- Below headline: subtitle in muted gray (#666) — "Tap one — we'll find the real need behind it" (this satisfies UX-01 inline guidance without blocking)
- FlatList with numColumns={2} displaying all 12 CRAVINGS

**Craving tile design:**
- Each tile: ~160px wide, ~90px tall, backgroundColor: '#1a1a1a', borderRadius: 12, padding: 12
- Emoji at top (fontSize: 28), label below (fontSize: 13, color: '#cccccc', fontWeight: '500')
- columnWrapperStyle: gap 10, contentContainerStyle: gap 10, horizontal padding 20
- activeOpacity: 0.7 on TouchableOpacity

**Navigation:**
- On tile press: `router.push({ pathname: '/result', params: { cravingId: item.id } })`
- Import router from 'expo-router', CRAVINGS from '../src/data/mappings'

**No loading states, no async operations on mount** — renders synchronously from imported data (same zero-async pattern from Phase 1).
  </action>
  <verify>npx tsc --noEmit passes with 0 errors; app renders without crashing (expo start and verify no red screen)</verify>
  <done>app/index.tsx shows headline + subtitle + 2-column grid of 12 craving tiles; tapping any tile navigates to /result with cravingId param; TypeScript clean</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npx tsc --noEmit passes with 0 errors
- [ ] src/data/mappings.ts exports CRAVINGS (12 items) and MAPPINGS (12 entries)
- [ ] app/index.tsx renders craving grid (FlatList, 2 columns)
- [ ] Tapping a tile calls router.push with cravingId param
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No TypeScript errors introduced
- Data layer and craving selection screen ready for Plan 02-02 (result screen)
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-redirect-flow/02-01-SUMMARY.md`
</output>
